import App from "../App";

describe.skip("App", () => {
  test("should filter the list of todos by completed", () => {
    console.log("🏋️‍♀️ 0 - Initial bootstrap logic.");

    // 💡 Make use of the render function.
    // Write the code here.

    console.log("👀 1 - Checking the output of the render method.");

    // 💡 Make use of the debug utility to check what is being rendered.

    console.log("🎯 2 - Getting filter and todo list");

    // 💡 Make use of the selector functions to get the following elements:
    // - list of todos;
    // - completed button;
    // - eat todo item checkbox;

    console.log("🧪 3 - Check the number of todos that are visible");

    // 💡 Make use of the `expect` assertion.

    console.log(
      "👷 4 - Filter the completed todos by clicking the completed button"
    );

    // 💡 Make use of the fireEvent utility to trigger the click.

    console.log("🧪 5 - Check the number of todos that are visible");

    // 💡 Make use of the `expect` assertion.

    console.log("👷 6 - Click on Eat todo item checkbox");

    // 💡 Make use of the fireEvent utility to trigger the click.

    console.log("🧪 7 - Check the number of todos that are visible");

    // 💡 Make use of the `expect` assertion.
  });

  test("should add a new item to the list of todos", () => {
    console.log("🏋️‍♀️ 0 - Initial bootstrap logic.");

    // 💡 Make use of the render function.
    // Write the code here.

    console.log("👀 1 - Checking the output of the render method.");

    // 💡 Make use of the debug utility to check what is being rendered.

    console.log("🎯 2 - Getting the form and todo list");

    // 💡 Make use of the selector functions to get the following elements:
    // - list of todos;
    // - add button;
    // - text area;

    console.log("🧪 3 - Check the number of todos that are visible");

    // 💡 Make use of the `expect` assertion.

    console.log("👷 3 - Type the new todo name.");

    // 💡 Make use of the userEvents utility to type the new name.

    console.log("👷 4 - Click the add button.");

    // 💡 Make use of the fireEvent utility to trigger the click.

    console.log("🧪 5 - Check the number of todos that are visible");

    // 💡 Make use of the `expect` assertion.
  });
});
